# =============================================================================
# Azure DevOps CI/CD Pipeline for FastAPI App Deployment to AWS EC2
# =============================================================================
# This pipeline builds a Docker image and deploys it to an AWS EC2 instance
# 
# PREREQUISITES:
# 1. Create an Azure DevOps project
# 2. Set up a Service Connection for SSH to EC2
# 3. Configure Pipeline Variables (see section below)
# =============================================================================

trigger:
  branches:
    include:
      - main
      - master
  paths:
    exclude:
      - README.md
      - docs/*

# Variables - Configure these in Azure DevOps Pipeline Variables (Settings)
# Mark sensitive values as "secret"
variables:
  # Docker settings
  dockerImageName: 'fastapi-demo-app'
  dockerTag: '$(Build.BuildId)'
  group: EC2-Deployment-Vars
  
  # EC2 settings - Set these in Azure DevOps Pipeline Variables:
  # EC2_HOST: Your EC2 public IP (e.g., 3.14.25.100)
  # SSH_SERVICE_CONNECTION: Name of your SSH service connection (e.g., EC2-SSH-Connection)

stages:
  # =============================================================================
  # STAGE 1: BUILD
  # =============================================================================
  - stage: Build
    displayName: 'Build & Test'
    jobs:
      - job: BuildJob
        displayName: 'Build Docker Image'
        pool:
          vmImage: 'ubuntu-latest'
        
        steps:
          # Checkout code
          - checkout: self
            displayName: 'Checkout Repository'

          # Install Python for testing
          - task: UsePythonVersion@0
            displayName: 'Use Python 3.11'
            inputs:
              versionSpec: '3.11'

          # Install dependencies and run tests
          - script: |
              python -m pip install --upgrade pip
              pip install -r requirements.txt
              pip install pytest httpx
            displayName: 'Install Dependencies'

          - script: |
              # Run simple health check test
              python -c "from app.main import app; print('✓ App imports successfully')"
            displayName: 'Validate App'

          # Build Docker image
          - task: Docker@2
            displayName: 'Build Docker Image'
            inputs:
              command: 'build'
              Dockerfile: 'Dockerfile'
              tags: |
                $(dockerImageName):$(dockerTag)
                $(dockerImageName):latest

          # Save Docker image as artifact
          - script: |
              docker save $(dockerImageName):$(dockerTag) -o $(Build.ArtifactStagingDirectory)/docker-image.tar
            displayName: 'Save Docker Image'

          # Publish Docker image artifact
          - task: PublishBuildArtifacts@1
            displayName: 'Publish Docker Image Artifact'
            inputs:
              PathtoPublish: '$(Build.ArtifactStagingDirectory)'
              ArtifactName: 'docker-image'
              publishLocation: 'Container'

          # Publish deployment scripts
          - task: PublishBuildArtifacts@1
            displayName: 'Publish Deployment Scripts'
            inputs:
              PathtoPublish: '$(System.DefaultWorkingDirectory)'
              ArtifactName: 'deploy-scripts'
              publishLocation: 'Container'

  # =============================================================================
  # STAGE 2: DEPLOY TO EC2
  # =============================================================================
  - stage: Deploy
    displayName: 'Deploy to EC2'
    dependsOn: Build
    condition: succeeded()
    jobs:
      - deployment: DeployToEC2
        displayName: 'Deploy to AWS EC2'
        pool:
          vmImage: 'ubuntu-latest'
        environment: 'production'  # Create this environment in Azure DevOps
        strategy:
          runOnce:
            deploy:
              steps:
                # Download artifacts
                - download: current
                  artifact: docker-image
                  displayName: 'Download Docker Image'

                - download: current
                  artifact: deploy-scripts
                  displayName: 'Download Deploy Scripts'

                # Deploy to EC2 via SSH
                # Option 1: Using SSH Service Connection
                - task: SSH@0
                  displayName: 'Setup Docker on EC2 (if needed)'
                  inputs:
                    sshEndpoint: '$(SSH_SERVICE_CONNECTION)'
                    runOptions: 'inline'
                    inline: |
                      echo "=== Checking if Docker is installed ==="
                      if ! command -v docker &> /dev/null; then
                        echo "Docker not found. Installing Docker..."
                        
                        # Detect OS and install Docker
                        if [ -f /etc/os-release ]; then
                          . /etc/os-release
                          OS=$ID
                        fi
                        
                        if [ "$OS" = "amzn" ] || [ "$OS" = "amazon" ]; then
                          echo "Installing Docker on Amazon Linux..."
                          sudo dnf update -y
                          sudo dnf install docker -y
                        elif [ "$OS" = "ubuntu" ]; then
                          echo "Installing Docker on Ubuntu..."
                          sudo apt update
                          sudo apt install -y docker.io
                        fi
                        
                        # Start and enable Docker
                        sudo systemctl start docker
                        sudo systemctl enable docker
                        sudo usermod -aG docker $USER
                        
                        echo "Docker installed successfully!"
                      else
                        echo "Docker is already installed"
                        docker --version
                      fi
                    readyTimeout: '60000'

                # Copy Docker image to EC2
                - task: CopyFilesOverSSH@0
                  displayName: 'Copy Docker Image to EC2'
                  inputs:
                    sshEndpoint: '$(SSH_SERVICE_CONNECTION)'
                    sourceFolder: '$(Pipeline.Workspace)/docker-image'
                    contents: 'docker-image.tar'
                    targetFolder: '/tmp'
                    cleanTargetFolder: false

                - task: SSH@0
                  displayName: 'Deploy Container on EC2'
                  inputs:
                    sshEndpoint: '$(SSH_SERVICE_CONNECTION)'
                    runOptions: 'inline'
                    inline: |
                      echo "=== Stopping existing containers ==="
                      sudo docker stop fastapi-demo-app || true
                      sudo docker rm fastapi-demo-app || true
                      
                      echo "=== Cleaning up old images ==="
                      sudo docker image prune -f
                      
                      echo "=== Loading new Docker image ==="
                      sudo docker load -i /tmp/docker-image.tar
                      
                      echo "=== Starting new container ==="
                      sudo docker run -d \
                        --name fastapi-demo-app \
                        --restart unless-stopped \
                        -p 8000:8000 \
                        -e ENVIRONMENT=production \
                        $(dockerImageName):$(dockerTag)
                      
                      echo "=== Verifying deployment ==="
                      sleep 5
                      curl -f http://localhost:8000/health || exit 1
                      
                      echo "=== Deployment successful! ==="
                    readyTimeout: '30000'

  # =============================================================================
  # STAGE 3: VERIFY DEPLOYMENT
  # =============================================================================
  - stage: Verify
    displayName: 'Verify Deployment'
    dependsOn: Deploy
    condition: succeeded()
    jobs:
      - job: HealthCheck
        displayName: 'Health Check'
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - script: |
              echo "Waiting for application to stabilize..."
              sleep 10
              
              echo "Checking health endpoint..."
              response=$(curl -s -o /dev/null -w "%{http_code}" http://$(EC2_HOST):8000/health)
              
              if [ "$response" = "200" ]; then
                echo "✓ Health check passed! Application is running."
                curl -s http://$(EC2_HOST):8000/api/info | python -m json.tool
              else
                echo "✗ Health check failed with status: $response"
                exit 1
              fi
            displayName: 'Verify Application Health'
